[package]
name = "blake3-lamport-signer"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
blake3-lamport-signatures = { path = ".." }
clap = { version = "4.2.4", features = ["derive"] }
